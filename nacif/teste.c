#include <stdio.h>
int main()
{
    int i, salvaOP;
    char verificaOP[6][10] = {"AND", "OR", "XOR", "NAND", "NOR", "MOR"};
    for (i = 0; i < 6; i++)
            if ("MOR" == "MOR")
                salvaOP = 6;
    printf("%d", salvaOP);
}